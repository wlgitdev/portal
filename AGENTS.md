## Environment
Before any work, every session: `bash scripts/dev-setup/setup.sh` (needs `MSSQL_SA_PASSWORD` in the environment).
Tests: Playwright (e2e/), run with `npx playwright test`.
Full human setup/run walkthrough (native Windows, and Linux/WSL2): see README.md.